{% extends 'layout.html.twig' %}

    {% block title %} Datos Personales - DevVideos {% endblock %}
{% block content %}
    <h1>Datos personales</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary"><i class="fas fa-check"></i> Guardar cambios</button>
    <a href="{{ path('cambio_clave') }}" class="btn btn-info"><i class="fas fa-lock"></i> Cambiar contraseña</a>
    <a href="{{ path('usuario_eliminar', {'id': app.user.id}) }}" class="btn btn-danger"><i class="fas fa-times"></i> Borrar Usaurio</a>
    {{ form_end(form) }}
{% endblock %}

{% block title %}
    Datos personales
{% endblock %}