{% extends 'layout.html.twig' %}

{% block title %}Modificar Categoria{% endblock %}

{% block content %}
    {% if not es_nueva %}
        <h1>Modificar Categoria</h1>
    {% else %}
        <h1>Nueva Categoria</h1>
    {% endif %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('categorias_listar') }}" class="btn btn-info"><i class="fas fa-arrow-left"></i> Volver</a>
    <button type="submit" class="btn btn-primary"><i class="fas fa-check"></i> Guardar Cambios</button>
    {% if not es_nueva %}<a href="{{ path('category_eliminar', {'id': categoria.id}) }}" class="btn btn-danger"><i class="fas fa-trash"></i> Eliminar Categoría</a>{% endif %}
    {{ form_end(form) }}

{% endblock %}