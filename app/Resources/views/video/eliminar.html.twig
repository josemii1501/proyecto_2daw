{% extends 'layout.html.twig' %}

{% block title %} Eliminar Video - DevVideos {% endblock %}

{% block content %}
    <h1 class="titulo_seccion">Eliminar Video</h1>

    <div class="alert alert-danger">
        ¡Atención! Esta operación no se puede deshacer.
        ¿Desea realmente eliminar el Video {{ video.title }}?
    </div>

    <form method="post">
        <a href="{{ path('video_modificar', {'id': video.id}) }}" class="btn btn-info"><i class="fas fa-arrow-left"></i> Volver</a>
        <button type="submit" class="btn btn-danger" name="borrar"><i class="fas fa-trash"></i> Confirmar</button>
    </form>
{% endblock %}